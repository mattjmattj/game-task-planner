{% extends 'base.html.twig' %}

{% block title %}TaskType index{% endblock %}

{% block body %}
    <h1>TaskType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for task_type in task_types %}
            <tr>
                <td>{{ task_type.id }}</td>
                <td>{{ task_type.name }}</td>
                <td>
                    <a href="{{ path('task_type_show', {'id': task_type.id}) }}">show</a>
                    <a href="{{ path('task_type_edit', {'id': task_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('task_type_new') }}">Create new</a>
{% endblock %}
